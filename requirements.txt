# Seismic/Vibration Intelligence — pipeline + API dependencies, the only
# requirements file in this repo (both the ML pipeline and the FastAPI
# service share one dependency set). Install with:
#   pip install -r requirements.txt

# ML / seismic waveform classification
torch==2.3.1
seisbench==0.7.0
obspy==1.4.0
scipy==1.13.1

# API service
fastapi==0.115.11
uvicorn[standard]==0.30.6
pydantic==2.9.2

psycopg2-binary==2.9.9
SQLAlchemy==2.0.25
python-dotenv==1.0.0
PyYAML==6.0.1
pandas==2.1.4
numpy==1.26.3

# Testing
pytest==7.4.4
